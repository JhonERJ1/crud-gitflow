const express = require("express");
const router = express.Router();

// Endpoint para verificar que el servidor esta activo.
router.get("/", (req, res) => {
  res.json({ status: "ok", timestamp: new Date().toISOString() });
});

module.exports = router;